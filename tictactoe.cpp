#include <iostream>

void printBoard(const unsigned int (&positions)[3][3]) {
  std::cout << " A | B | C \n         \n---|---|---\n " << positions[0][0];
}
  
int main()
{
  unsigned char positions[3][3] = {' '};

  bool running = true;
  while (running) {
      
  }
  
  return 0;
}
